name: fabric-x


networks:
  fabric-x:
    name: fabric-x
    driver: bridge

volumes:
  committer-<%= fabricX.applicationOrgSlug %>-db-data:

services:

  # ─── Orderer Party 1 ───────────────────────────────────────────────────────
  orderer-router:
    container_name: orderer-router
    image: ghcr.io/hyperledger/fabric-x-orderer:1.0.0
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: router --config=/config/node_config.yaml
    ports:
      - "7050:7050"
    volumes:
      - ./config/party1-router.yaml:/config/node_config.yaml:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/router.orderer.example.com/tls:/node-tls:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/router.orderer.example.com/msp:/node-msp:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/msp/tlscacerts/tlsca.orderer.example.com-cert.pem:/org-tls-ca.pem:ro,Z
      - ./crypto/client-tls-ca.pem:/client-tls-ca.pem:ro,Z
      - ./crypto/config-block.pb.bin:/bootstrap/genesis.block:ro,Z
      - ./data/orderers/party1-router:/data:Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9443/metrics"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    depends_on:
      orderer-assembler:
        condition: service_healthy
    networks:
      - fabric-x

  orderer-batcher:
    container_name: orderer-batcher
    image: ghcr.io/hyperledger/fabric-x-orderer:1.0.0
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: batcher --config=/config/node_config.yaml
    ports:
      - "7051:7051"
    volumes:
      - ./config/party1-batcher.yaml:/config/node_config.yaml:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/batcher1.orderer.example.com/tls:/node-tls:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/batcher1.orderer.example.com/msp:/node-msp:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/msp/tlscacerts/tlsca.orderer.example.com-cert.pem:/org-tls-ca.pem:ro,Z
      - ./crypto/config-block.pb.bin:/bootstrap/genesis.block:ro,Z
      - ./data/orderers/party1-batcher:/data:Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9443/metrics"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    depends_on:
      orderer-consenter:
        condition: service_healthy
    networks:
      - fabric-x

  orderer-consenter:
    container_name: orderer-consenter
    image: ghcr.io/hyperledger/fabric-x-orderer:1.0.0
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: consensus --config=/config/node_config.yaml
    ports:
      - "7052:7052"
    volumes:
      - ./config/party1-consenter.yaml:/config/node_config.yaml:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/consenter.orderer.example.com/tls:/node-tls:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/consenter.orderer.example.com/msp:/node-msp:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/msp/tlscacerts/tlsca.orderer.example.com-cert.pem:/org-tls-ca.pem:ro,Z
      - ./crypto/config-block.pb.bin:/bootstrap/genesis.block:ro,Z
      - ./data/orderers/party1-consenter:/data:Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9443/metrics"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    networks:
      - fabric-x

  orderer-assembler:
    container_name: orderer-assembler
    image: ghcr.io/hyperledger/fabric-x-orderer:1.0.0
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: assembler --config=/config/node_config.yaml
    ports:
      - "7053:7053"
    volumes:
      - ./config/party1-assembler.yaml:/config/node_config.yaml:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/assembler.orderer.example.com/tls:/node-tls:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/orderers/party1/assembler.orderer.example.com/msp:/node-msp:ro,Z
      - ./crypto/ordererOrganizations/orderer.example.com/msp/tlscacerts/tlsca.orderer.example.com-cert.pem:/org-tls-ca.pem:ro,Z
      - ./crypto/client-tls-ca.pem:/client-tls-ca.pem:ro,Z
      - ./crypto/config-block.pb.bin:/bootstrap/genesis.block:ro,Z
      - ./data/orderers/party1-assembler:/data:Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9443/metrics"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
    depends_on:
      orderer-batcher:
        condition: service_healthy
    networks:
      - fabric-x





  # ─── <%= fabricX.applicationOrg.name %> Committer Stack ───────────────────────────────────────────────────

  committer-<%= fabricX.applicationOrgSlug %>-db:
    image: docker.io/library/postgres:18.3-alpine3.23
    command: postgres -c max_connections=500
    pull_policy: missing
    restart: unless-stopped
    environment:
      POSTGRES_DB: fabricx
      POSTGRES_USER: fabricx
      POSTGRES_PASSWORD: fabricx
      PGDATA: /var/lib/postgresql/data
    volumes:
      - committer-<%= fabricX.applicationOrgSlug %>-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fabricx -d fabricx"]
      interval: 5s
      timeout: 5s
      retries: 30
    networks:
      - fabric-x

  committer-<%= fabricX.applicationOrgSlug %>-verifier:
    image: ghcr.io/hyperledger/fabric-x-committer:1.0.3
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: start verifier --config=/config/verifier.yaml
    volumes:
      - ./config/verifier.yaml:/config/verifier.yaml:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/peers/committer-verifier.<%= fabricX.applicationOrg.domain %>/tls:/node-tls:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/msp/tlscacerts/tlsca.<%= fabricX.applicationOrg.domain %>-cert.pem:/org-tls-ca.pem:ro,Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:2115/metrics"]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 10s
    networks:
      - fabric-x

  committer-<%= fabricX.applicationOrgSlug %>-validator:
    image: ghcr.io/hyperledger/fabric-x-committer:1.0.3
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: start vc --config=/config/validator.yaml
    volumes:
      - ./config/validator.yaml:/config/validator.yaml:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/peers/committer-validator.<%= fabricX.applicationOrg.domain %>/tls:/node-tls:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/msp/tlscacerts/tlsca.<%= fabricX.applicationOrg.domain %>-cert.pem:/org-tls-ca.pem:ro,Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:2116/metrics"]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 10s
    depends_on:
      committer-<%= fabricX.applicationOrgSlug %>-db:
        condition: service_healthy
    networks:
      - fabric-x

  committer-<%= fabricX.applicationOrgSlug %>-coordinator:
    image: ghcr.io/hyperledger/fabric-x-committer:1.0.3
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: start coordinator --config=/config/coordinator.yaml
    volumes:
      - ./config/committer-coordinator.yaml:/config/coordinator.yaml:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/peers/committer-coordinator.<%= fabricX.applicationOrg.domain %>/tls:/node-tls:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/msp/tlscacerts/tlsca.<%= fabricX.applicationOrg.domain %>-cert.pem:/org-tls-ca.pem:ro,Z
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:2119/metrics"]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 10s
    depends_on:
      committer-<%= fabricX.applicationOrgSlug %>-verifier:
        condition: service_healthy
      committer-<%= fabricX.applicationOrgSlug %>-validator:
        condition: service_healthy
    networks:
      - fabric-x

  committer-<%= fabricX.applicationOrgSlug %>-sidecar:
    image: ghcr.io/hyperledger/fabric-x-committer:1.0.3
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: start sidecar --config=/config/sidecar.yaml
    ports:
      - "4001:4001"
    volumes:
      - ./config/sidecar.yaml:/config/sidecar.yaml:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/peers/committer-sidecar.<%= fabricX.applicationOrg.domain %>/tls:/node-tls:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/msp/tlscacerts/tlsca.<%= fabricX.applicationOrg.domain %>-cert.pem:/org-tls-ca.pem:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/users/User1@<%= fabricX.applicationOrg.domain %>/msp:/msp:ro,Z
      - ./crypto/ordererOrganizations:/orderer-cas:ro,Z
      - ./crypto/config-block.pb.bin:/bootstrap/genesis.block:ro,Z
      - ./data/committer-<%= fabricX.applicationOrgSlug %>/sidecar-ledger:/ledger:Z
    healthcheck:
      test: ["CMD", "curl", "-sk", "--cert", "/node-tls/server.crt", "--key", "/node-tls/server.key", "https://localhost:2114/metrics"]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 10s
    depends_on:
      committer-<%= fabricX.applicationOrgSlug %>-coordinator:
        condition: service_healthy

      orderer-router:
        condition: service_healthy

    networks:
      - fabric-x

  committer-<%= fabricX.applicationOrgSlug %>-query-service:
    image: ghcr.io/hyperledger/fabric-x-committer:1.0.3
    pull_policy: missing
    restart: unless-stopped
    user: "${FABRIC_X_UID:-1000}:${FABRIC_X_GID:-1000}"
    command: start query --config=/config/query-service.yaml
    ports:
      - "7001:7001"
    volumes:
      - ./config/query-service.yaml:/config/query-service.yaml:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/peers/committer-query-service.<%= fabricX.applicationOrg.domain %>/tls:/node-tls:ro,Z
      - ./crypto/peerOrganizations/<%= fabricX.applicationOrg.domain %>/msp/tlscacerts/tlsca.<%= fabricX.applicationOrg.domain %>-cert.pem:/org-tls-ca.pem:ro,Z
    depends_on:
      committer-<%= fabricX.applicationOrgSlug %>-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:2117/metrics"]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 10s
    networks:
      - fabric-x
