#!/bin/bash

set -e

CONFIG="${CHAINCODE_AS_A_SERVICE_BUILDER_CONFIG:-{}}"
echo "Releasing chaincode using config: $CONFIG" >&2

mkdir -p /tmp/ccaas_extratct
tar -xzf code.tar.gz -C /tmp/ccaas_extratct

cat /tmp/ccaas_extratct/connection.json